# Usos y productos (data_04_usos)

- `reportes/reporte_resumen.py`: genera CSVs (`tmp/reporte_ventas.csv`, `tmp/reporte_pagos.csv`) a partir de `dm_sales_diarias` y `dm_pagos_diarios` para mostrar consumos simples de los datos consolidados.

Sirve para enseñar cómo exponer un producto ligero consumible por analistas o dashboards.
