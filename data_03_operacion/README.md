# Operación y monitoreo (data_03_operacion)

- `monitoreo/resumen_ejecuciones.py`: lee `control_ejecuciones`, `dm_sales_diarias` y `dm_pagos_diarios` y genera `tmp/monitoreo_resumen.csv` para tener un snapshot operativo.

Útil para mostrar cómo cerrar el ciclo de observabilidad después de las cargas y consolidaciones.
